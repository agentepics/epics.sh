---
spec_version: 0.5.1
id: install-hook-epic
---

# Install Hook Epic

## Objective

Exercise the `install` trigger during `epics install`.

## Resume

Check `runtime/install-hook-output.json` for the last install-hook payload.
