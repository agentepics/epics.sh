# Install Hook Epic

## Purpose

Validate that `epics install` executes the EPIC-spec `install` trigger.

## Rules

- Treat the install hook as part of first activation.
- Write deterministic runtime evidence for E2E validation.
