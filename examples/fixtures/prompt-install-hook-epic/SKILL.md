# Prompt Install Hook Epic

## Purpose

Validate that a real Claude-backed `.md` install hook runs during `epics install`.

## Rules

- Write a deterministic runtime artifact during install.
- Do not depend on any external project files.
