---
spec_version: 0.5.1
id: prompt-install-hook-epic
---

# Prompt Install Hook Epic

## Objective

Exercise the prompt-based `install` trigger with a deterministic runtime write.

## Resume

Check `runtime/prompt-hook-output.json` for the install-hook result.
