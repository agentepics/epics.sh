# Current Plan

## Now

Each run should stay in flight for about 20 seconds.
Use the shell tool and run exactly `python3 -c "import time; time.sleep(20)"`.
Wait for that shell command to finish before responding.
When the sleep finishes, respond exactly `DONE`.
Do not change files or runtime state.
