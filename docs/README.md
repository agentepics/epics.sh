# Docs

This directory holds project docs for `epics.sh`.

Sections:

- `planning/` for roadmap and execution plans
- `planning/WEB_PRD.md` for the website product requirements draft
- `architecture/` for daemon and runtime notes
- `specification/` for possible spec-facing extensions
- `research/` for compact research snapshots
- `adapters/` for host-specific adapter research
